# 贡献指南

感谢你愿意为 Frontend Knowledge 项目贡献知识！本文档将帮助你了解如何贡献高质量的知识内容。

---

## 📋 贡献类型

| 类型 | 描述 | 奖励 |
|-----|------|------|
| **新增模式** | 添加新的技术模式 | ⭐⭐⭐ |
| **完善现有** | 补充现有模式的示例或说明 | ⭐⭐ |
| **踩坑记录** | 记录有价值的问题和解决方案 | ⭐⭐ |
| **修正错误** | 修正过时或错误的内容 | ⭐ |
| **优化规范** | 改进知识规范或模板 | ⭐⭐ |

---

## 🚀 贡献流程

### Step 1: 发现知识

在哪里可以找到值得贡献的知识？

```
✅ 好的知识来源：
├── 项目中好的代码实现
├── 解决过的问题和方案
├── 团队分享的最佳实践
├── 踩过的坑和经验教训
└── 技术选型的思考过程

❌ 不适合贡献：
├── 过于通用的知识（单个函数用法）
├── 业务相关的专有逻辑
├── 未经验证的想法
└── 复制粘贴的文档
```

### Step 2: 触发蒸馏

使用 `frontend-distill` Skill 触发蒸馏：

```markdown
# 场景 1: 发现好的代码实现
用户: "这段动画代码很好，帮我蒸馏成模式"

# 场景 2: 总结最佳实践
用户: "总结一下虚拟列表的最佳实践"

# 场景 3: 记录踩坑
用户: "记录一下这个性能问题"

# 场景 4: 验证知识质量
用户: "帮我验证这篇文档的完整性"
```

### Step 3: 填写模板

按照对应的模板填写内容：

| 知识类型 | 推荐模板 |
|---------|---------|
| 技术实现模式 / 动效 / 布局 / 功能 | `distill-guide.md`（按 type 选对应章节） |
| 经验总结 | `distill-guide.md`（best 章节） |
| 问题记录 | `distill-guide.md`（pitfall 章节） |
| 页面布局模式 | `page-layout-template.md` |

### Step 4: 提交验证

使用 `verify:guide` 验证知识质量：

```bash
# 基本验证
verify:guide [path] --check-level basic

# 标准验证
verify:guide [path] --check-level standard

# 严格验证
verify:guide [path] --check-level strict
```

### Step 5: 保存与同步

验证通过后，知识会自动保存并同步到：

- Wiki 目录
- 域索引
- 关键词表
- 语义标签

---

## 📝 知识规范

### 文件命名

```
✅ 正确示例：
├── gsap页面过渡.md
├── scroll-trigger.md
├── 虚拟列表.md
└── responsive-grid.md

❌ 错误示例：
├── GSAP动画完整指南.md（太长）
├── a.md（无意义）
└── 新建文件夹（1）.md（含特殊字符）
```

### Frontmatter

每个知识文档必须包含以下 Frontmatter：

```markdown
---
title: "[模式名称]"           # 必填，一句话描述
description: "[描述]"          # 必填，详细描述
domain: [域]                  # 必填：动效/布局/功能/项目规范/设计主题/技术选型
type: [类型]                  # 必填：pattern/best-practice/pitfall
tags: ["标签1", "标签2"]     # 必填，用于匹配
complexity: ⭐⭐               # 推荐，复杂度 1-5 星
created: "[日期]"              # 必填
---
```

### 内容要求

```
✅ 必填部分：
├── 概述（2-3句话描述）
├── 适用场景（适合/不适合）
├── 快速使用（基础示例）
├── 核心代码
└── 注意事项

⚠️ 推荐部分：
├── 进阶用法
├── 常见问题
├── 性能考虑
├── 相关模式
└── 参考资料
```

---

## 🎯 质量标准

好的知识应该：

```
✅ 完整性
├── 包含所有必要部分
├── 步骤清晰完整
└── 代码可独立运行

✅ 准确性
├── API 版本正确
├── 概念定义准确
└── 无过时内容

✅ 可实现性
├── 代码可直接运行
├── 环境要求明确
└── 依赖版本说明

✅ 可维护性
├── 包含更新日期
├── 维护责任人
└── 易于更新扩展
```

---

## ⚠️ 注意事项

### 不要贡献

```
❌ 过于通用的知识
   └── 单个函数的用法（如 useState 的用法）

❌ 业务相关的专有逻辑
   └── 公司特定的业务流程

❌ 未经验证的方案
   └── 未经测试的想法

❌ 复制粘贴的文档
   └── 官方文档的重复内容
```

### 知识粒度

```
✅ 正确粒度：
├── 中等粒度：可独立使用的模式
├── 示例："虚拟列表"、"响应式栅格"、"表单验证"
└── 特点：有明确的输入输出，可独立实现

❌ 粒度过细：
├── 单个函数或 Hook
├── 示例："useState 用法"
└── 问题：缺乏复用价值

❌ 粒度过粗：
├── 完整页面或复杂系统
├── 示例："用户中心"
└── 问题：难以复用，应拆分
```

---

## 🔍 审查清单

提交前请检查：

```
□ 知识有明确的价值（解决实际问题）
□ 内容完整（包含必要部分）
□ 代码可运行（无语法错误）
□ 说明清晰（易于理解）
□ 格式规范（符合模板）
□ 关联完整（链接相关知识）
□ 标签准确（用于匹配）
□ 依赖明确（版本要求）
```

---

## 📞 常见问题

### Q: 如何确定知识粒度？

A: 如果一个知识可以被多个项目复用，就是合适的粒度。如果只在一个地方用到，可能粒度过细；如果包含多个可独立复用的部分，可能粒度过粗。

### Q: 踩坑记录应该写多详细？

A: 应该包含：问题描述、原因分析、解决方案（多种）、预防措施。重点是让他人能够复现问题和解决问题。

### Q: 如何更新过时的知识？

A: 检查依赖版本、API 变化、最佳实践演进。更新后注明版本历史和更新原因。

### Q: 发现错误怎么办？

A: 立即修正！包括：内容错误、链接失效、代码过时、示例不完整等。

---

## 🏆 贡献者

感谢所有贡献者！

| 贡献者 | 贡献内容 | 日期 |
|-------|---------|------|
| AI Assistant | 初始知识库搭建 | 2024-01 |

---

## 📧 联系我们

如有问题或建议：

- 在项目中提交 Issue
- 联系维护者

---

*感谢你的贡献！每一份知识都在帮助团队成长。*
