# Frontend Knowledge Agent

前端技术经验知识库和蒸馏系统。用于管理和复用前端项目的技术实现经验。

---

## 📖 目录

1. [项目介绍](#1-项目介绍)
   - [核心约束](#12-核心约束禁止向其他-skill-蒸馏知识)
   - [自动触发模式](#14-自动触发模式)
2. [快速开始](#2-快速开始)
3. [命令系统](#3-命令系统)
4. [知识域分类](#4-知识域分类)
5. [蒸馏知识流程](#5-蒸馏知识流程)
6. [知识规范](#6-知识规范)
7. [知识文档模板](#7-知识文档模板)
8. [验证机制](#8-验证机制)
9. [贡献指南](#9-贡献指南)

---

## 1. 项目介绍

### 1.1 项目目标

构建一个**可复用**的前端技术经验知识库，让 AI 在开发前端页面时能够自动借鉴团队经验，实现：

- ✅ 动效、布局、功能等技术点的快速复用
- ✅ 团队最佳实践的标准化和传承
- ✅ 踩坑经验的记录和避免
- ✅ 技术选型的参考和决策支持

### 1.2 ⚠️ 核心约束：禁止向其他 Skill 蒸馏知识

> **本项目是且仅是 `frontend` 这个 Skill 的内容维护工具。**
>
> 所有知识蒸馏操作（`/frontend:distill`）**必须且只能**保存到 `frontend` skill 的对应知识域目录中。
>
> **严禁**将任何前端技术知识蒸馏到其他 Skill、其他目录或本项目以外的任何位置。
>
> 违反此约束会导致：
> - 知识分散，无法通过 `/frontend` 检索
> - 索引体系失效，经验无法复用
> - 知识库维护混乱，重复覆盖
>
> 如有疑问，知识蒸馏的目标永远是：`__SKILL_ROOT__\knowledge\[对应域]/`（`__SKILL_ROOT__` 由安装脚本替换为实际路径）

### 1.3 项目架构

```
frontend-knowledge/              # GitHub 仓库根
│
├── install.ps1                # 跨工具安装脚本（TRAE / Claude Code / DSH）
├── SKILL.md                  # Claude Code 项目级入口（复制为 CLAUDE.md 使用）
├── CLAUDE.md                 # Claude Code 项目级 Skill 入口（同上，兼容性备用）
├── README.md                  # 英文安装说明
├── README-zh.md              # 中文安装说明
│
├── frontend/                  # Skill 内容目录（名字 = Skill 名 for Claude Code）
│   ├── SKILL.md             # 主入口 + 自动触发模式说明
│   ├── agent.md             # Agent 上下文约束
│   │
│   ├── commands/             # 命令参考
│   │   ├── frontend.md     # /frontend 详解（需求描述 + 页面路径 双分支）
│   │   ├── distill.md      # /frontend:distill 详解（含 page-layout 类型）
│   │   └── list.md        # /frontend:list 详解
│   │
│   ├── templates/            # 知识模板
│   │   ├── distill-guide.md        # 通用知识蒸馏指南（按 type 选用对应章节）
│   │   ├── analyze-output.md       # /frontend <path> 输出模板
│   │   └── page-layout-template.md # 页面布局模式蒸馏专用模板
│   │
│   ├── verification/         # 验证机制
│   │   ├── analyze-checklist.md
│   │   ├── distill-checklist.md
│   │   ├── index-sync-checklist.md
│   │   └── knowledge-quality-checklist.md
│   │
│   └── knowledge/            # 知识库（6 域）
│       ├── 动效/
│       ├── 布局/
│       ├── 功能/
│       ├── 项目规范/
│       ├── 设计主题/
│       ├── 技术选型/
│       └── 索引/           # L1-L5 五层召回索引
│
└── .claude-plugin/          # Claude Code 插件元数据（发布用）
    └── manifest.json
```

### 1.4 核心功能

| 命令 | 功能 | 使用场景 |
|-----|------|---------|
| **`/frontend`** | 知识查询 + 页面分析（双分支） | `/frontend <需求描述>` 搜索知识；`/frontend <页面路径>` 分析已有页面 |
| **`/frontend:distill`** | 知识蒸馏 | 从代码或项目中提取和保存知识（pattern / best / pitfall / page-layout） |
| **`/frontend:list`** | 知识列表 | 查看知识库中有哪些技能 |
| **自动触发** | 上下文识别 | 开发 UI / 加动画 / 做布局 / 写组件 / 处理交互 / 实现需求 / 改造页面时自动匹配知识库，无需显式调用 |

---

## 2. 快速开始

### 2.1 环境要求

- TRAE IDE 或支持 Skill 的 AI 编码工具
- 了解前端基础知识（React/Vue/CSS）

### 2.2 命令速查

| 需求 | 命令 |
|-----|------|
| 列出所有知识 | `/frontend:list` |
| 列出动效域 | `/frontend:list --domain 动效` |
| 查找页面过渡动画 | `/frontend 页面过渡动画` |
| 查找虚拟列表实现 | `/frontend 虚拟列表` |
| 查找响应式布局方案 | `/frontend 响应式布局` |
| 分析已有页面并生成改造方案 | `/frontend src/pages/ParcelManagement/index.tsx` |
| 从代码提取模式 | `/frontend:distill src/components/Modal.tsx --type pattern --domain 动效` |
| 蒸馏页面布局模式 | `/frontend:distill src/pages/OrderList/index.tsx --type page-layout --domain 布局` |
| 记录踩坑 | `/frontend:distill --type pitfall --domain 动效 --name "动画卡顿"` |
| 提炼最佳实践 | `/frontend:distill --type best --domain 功能 --name "虚拟列表最佳实践"` |

### 2.3 使用流程

#### 路径 A：自动触发（推荐）

```
用户说"加一个入场动画" / "改造登录页" 等带意图的话
    ↓
TRAE 扫描 Skill YAML，命中 frontend 的自动触发关键词
    ↓
┌──────────────────────────────────────┐
│ 自动路由到 /frontend                 │
│   - 需求描述 → 走需求描述分支         │
│   - 页面路径 → 走页面路径分支         │
└──────────────┬───────────────────────┘
               ↓
         召回 + 编排 + 输出提示词
```

#### 路径 B：显式调用

```
开发需求
    ↓
┌────────────────────────────┐
│ /frontend <需求描述>       │
│ 或 /frontend <页面路径>    │
│ 搜索 / 分析相关知识          │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ 查看召回方案                │
│ 应用代码和方案              │
└────────────┬───────────────┘
             ↓
    发现好的实现？
    ↓           ↓
   是           否
    ↓           ↓
┌────────────────────────────┐
│ /frontend:distill          │
│ 蒸馏并保存到 knowledge/    │
└────────────────────────────┘
```

---

## 3. 命令系统

### 3.1 /frontend - 主命令（双分支）

**当前 Skill 的唯一主命令**，承担原 `/frontend:knowledge`（关键词搜索 + 知识匹配）与 `/frontend:analyze`（页面分析 + 方案编排）的全部能力。

#### 命令格式

```
/frontend <需求描述 | 页面路径>
```

#### 输入识别规则

执行前先识别输入意图：

```
输入
    ↓
┌──────────────────────────────────────┐
│ Step 1：判断输入类型                  │
│  ├── 是已存在的本地文件/目录路径      │
│  │    → 命中"页面路径分支"           │
│  └── 否则                             │
│       → 命中"需求描述分支"           │
└──────────────────────────────────────┘
```

**优先级**：同一输入同时含路径和需求时，**优先页面路径分支**（路径即硬证据）。

#### 分支一：需求描述

| 项 | 说明 |
|----|------|
| 适用场景 | 想搜索某个效果怎么实现；按知识库最佳实践实现新功能；优化某个交互/动效/布局/主题；对比技术方案 |
| 召回架构 | L1 精确匹配 → L2 语义匹配 → L3 扩展匹配 → L4 组合匹配 → L5 场景推荐 |
| 输出形态 | 召回方案列表 + 知识摘要 + Agent 实施提示词 |

#### 分支二：页面路径

| 项 | 说明 |
|----|------|
| 适用场景 | 已有页面需要补齐布局/排版/视觉；需要保留业务逻辑做改造；需要生成自包含提示词给实现 Agent |
| 输出形态 | 页面分析报告 + Agent 实施提示词 |

#### 使用示例

```
/frontend 实现一个带入场动画的筛选表格页面
/frontend 优化卡片列表的过渡动画
/frontend 给登录页加上暗色模式
/frontend src/pages/ParcelManagement/index.tsx
/frontend src/pages/CustomerList/index.tsx
```

> 完整规则详见 `commands/frontend.md`。

### 3.2 /frontend:distill - 知识蒸馏

> ⚠️ **路径硬约束**：所有落盘操作**只能且必须**写入 `__SKILL_ROOT__\knowledge\[对应域]/`，严禁写入其他目录。

#### 命令格式

```
/frontend:distill [源路径] --type [类型] --domain [域] [--name 名称]
```

#### 参数说明

| 参数 | 必填 | 说明 | 可选值 |
|-----|------|------|--------|
| `源路径` | 条件 | 代码文件或目录路径 | 文件路径、目录路径 |
| `--type` | 是 | 蒸馏类型 | `pattern` / `best` / `pitfall` / `page-layout` |
| `--domain` | 是 | 知识域 | `动效` / `布局` / `功能` / `项目规范` / `设计主题` / `技术选型` |
| `--name` | 否 | 模式名称 | 自定义名称 |

#### 蒸馏类型

| 类型 | 用途 | 示例 |
|-----|------|------|
| `pattern` | 从代码提取可复用的技术模式 | `/frontend:distill src/components/VirtualList.tsx --type pattern --domain 功能` |
| `best` | 提炼最佳实践和经验 | `/frontend:distill --type best --domain 动效 --name "GSAP性能优化"` |
| `pitfall` | 记录踩坑和解决方案 | `/frontend:distill --type pitfall --domain 动效 --name "动画卡顿"` |
| `page-layout` | 蒸馏页面布局模式（独立流程） | `/frontend:distill src/pages/OrderList/index.tsx --type page-layout --domain 布局` |

#### 使用示例

```
/frontend:distill src/components/Modal.tsx --type pattern --domain 动效
/frontend:distill --type best --domain 动效 --name "GSAP性能优化"
/frontend:distill --type pitfall --domain 动效 --name "动画卡顿问题"
/frontend:distill src/pages/OrderList/index.tsx --type page-layout --domain 布局
```

> 完整规则详见 `commands/distill.md`。

### 3.3 /frontend:list - 知识列表

#### 命令格式

```
/frontend:list [--domain 域]
```

#### 使用示例

```
/frontend:list
/frontend:list --domain 动效
/frontend:list --domain 布局
```

### 3.4 自动触发模式

**Skill 安装后默认开启自动触发**，无需显式调用 `/frontend` 即可在 TRAE 中匹配知识库。

#### 触发关键词（YAML description 中声明）

| 域 | 触发词 |
|----|--------|
| 布局 | UI 页面、布局、改造页面 |
| 动效 | 动画效果 |
| 功能 | 组件、交互 |
| 跨域 | 实现需求 |

#### 自动触发识别流程

```
用户输入带意图的自然语言 / 页面路径
    ↓
TRAE 扫描 Skill YAML description 关键词命中
    ↓
┌──────────────────────────────────────┐
│ 自动路由到 /frontend                 │
│   - 含页面路径 → 走页面路径分支       │
│   - 否则      → 走需求描述分支       │
└──────────────┬───────────────────────┘
               ↓
        L1-L5 五层召回 + 输出

```

#### 关闭自动触发

- **临时关闭**：用户在该轮对话中显式声明"不要走 Skill"
- **永久关闭**：在项目根目录的 TRAE 配置文件中加入 `skill.frontend.auto-trigger: false`

#### 与显式调用的关系

| 方式 | 触发时机 | 推荐场景 |
|------|---------|---------|
| 自动触发 | 用户说"做 XX"、"改 XX"等带意图的描述 | 日常开发意图 |
| 显式 `/frontend` | 用户主动输入命令 | 已知要查什么、复杂需求 |
| 显式 `/frontend:distill` | 用户主动输入命令 | 蒸馏新知识 |

> 详细关键词表与边界规则见 `SKILL.md` → "自动触发模式（Auto-Trigger）"章节。

---

## 4. 知识域分类

### 4.1 六大知识域

| 域 | 命令参数 | 内容 | 示例 |
|---|---------|------|------|
| **动效** | `--domain 动效` | 动画、过渡、转场 | GSAP 页面过渡、ScrollTrigger、骨架屏 |
| **布局** | `--domain 布局` | 响应式、栅格、定位 | 响应式栅格、Sticky 侧边栏、瀑布流 |
| **功能** | `--domain 功能` | 组件、交互、状态 | 虚拟列表、表单验证、拖拽排序 |
| **项目规范** | `--domain 项目规范` | 命名、结构、Git | 组件命名、提交规范 |
| **设计主题** | `--domain 设计主题` | 色彩、字体、主题 | 色彩系统、暗色模式 |
| **技术选型** | `--domain 技术选型` | 库对比、方案选择 | Redux vs Zustand、GSAP vs Framer |

### 4.2 触发关键词

#### 动效域

| 关键词 | 同义词 | 语义变体 |
|--------|--------|---------|
| 页面过渡 | 转场、路由切换 | "换页"、"跳转" |
| 入场动画 | 淡入、滑入 | "出现"、"进来" |
| 加载动画 | loading、骨架屏 | "等一下"、"加载中" |
| 滚动动画 | 视差、scroll | "滚动时动"、"滑动" |
| 交互反馈 | hover、点击效果 | "放上去"、"点一下" |

#### 布局域

| 关键词 | 同义词 | 语义变体 |
|--------|--------|---------|
| 响应式 | 适配、自适应 | "手机能看"、"多端适配" |
| 栅格 | grid、flex | "格子"、"分列" |
| 居中 | 垂直居中、水平居中 | "放中间" |
| 侧边栏 | sidebar、sticky | "固定在旁边" |
| 瀑布流 | masonry、错位 | "图片不整齐" |

#### 功能域

| 关键词 | 同义词 | 语义变体 |
|--------|--------|---------|
| 虚拟列表 | virtual list | "大数据"、"列表卡" |
| 分页 | pagination、翻页 | "一页多少条" |
| 无限滚动 | loadmore、触底加载 | "滚到底加载" |
| 拖拽排序 | drag sort | "可以拖动" |
| 表单验证 | 表单校验 | "检查输入" |

---

## 5. 蒸馏知识流程

### 5.1 完整蒸馏流程

```
┌────────────────────────────┐
│ Step 1: 分析源内容         │
│                              │
│ 代码文件：                    │
│ - 识别核心功能               │
│ - 提取关键实现               │
│ - 分析依赖关系               │
│ - 识别使用场景               │
│                              │
│ 最佳实践：                    │
│ - 确定主题                   │
│ - 收集相关信息               │
│ - 提炼核心要点               │
│                              │
│ 踩坑记录：                   │
│ - 描述问题现象               │
│ - 分析问题原因               │
│ - 总结解决方案               │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Step 2: 选择模板             │
│                              │
│ pattern → distill-guide.md（pattern 章节）  │
│ best → distill-guide.md（best 章节）       │
│ pitfall → distill-guide.md（pitfall 章节） │
│
│ page-layout 独立流程，使用 page-layout-template.md（见 commands/distill.md） │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Step 3: 提取知识             │
│                              │
│ - 核心实现                   │
│ - 关键参数                   │
│ - 使用场景                   │
│ - 注意事项                   │
│ - 依赖要求                   │
│ - 性能考虑                   │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Step 4: 验证                │
│                              │
│ ✅ 代码完整性                │
│ ✅ 步骤清晰度               │
│ ✅ 依赖明确性               │
│ ✅ 示例可运行               │
└────────────┬───────────────┘
             ↓
┌────────────────────────────┐
│ Step 5: 保存与同步           │
│                              │
│ - 保存到 knowledge/[域]/    │
│ - 更新域索引                │
│ - 同步关键词表              │
│ - 更新语义标签              │
└────────────────────────────┘
```

### 5.2 触发场景

| 场景 | 推荐命令 |
|-----|---------|
| 发现好的代码实现需要复用 | `/frontend:distill [path] --type pattern` |
| 总结某个技术的最佳实践 | `/frontend:distill --type best` |
| 记录踩坑和解决方案 | `/frontend:distill --type pitfall` |
| 提取项目经验 | `/frontend:distill [path] --type pattern` |

---

## 6. 知识规范

### 6.1 文件命名规范

```
规则:
├── 使用小写字母和连字符
├── 示例: gsap页面过渡.md, scroll-trigger.md, 虚拟列表.md
├── 避免: 中文文件名太长，使用英文或简短中文
└── 一致性: 同类型文件使用相同命名风格
```

### 6.2 Frontmatter 规范

```markdown
---
title: "[模式名称]"              # 必填，一句话描述
description: "[描述]"             # 必填，详细描述
domain: [域]                     # 必填：动效/布局/功能/项目规范/设计主题/技术选型
type: [类型]                    # 必填：pattern/best-practice/pitfall
tags: ["标签1", "标签2"]        # 必填，用于匹配
complexity: ⭐⭐                  # 推荐，复杂度 1-5 星
dependencies:                     # 推荐，依赖的库
  - name: "[库名]"
    version: "[版本]"
techStack: [React/Vue/通用]     # 推荐，技术栈
relatedPatterns:                 # 推荐，关联的模式
  - "[模式1]"
  - "[模式2]"
created: "[日期]"                # 必填
updated: "[日期]"                # 推荐
author: "[作者]"                 # 推荐
---
```

### 6.3 内容结构规范

```
✅ 必填部分:
├── 概述（2-3句话描述）
├── 适用场景（适合/不适合）
├── 快速使用（基础示例）
├── 核心代码
└── 注意事项

⚠️ 推荐部分:
├── 进阶用法
├── 常见问题
├── 性能考虑
├── 相关模式
└── 参考资料

📝 可选部分:
├── 完整示例
├── 测试代码
├── 版本历史
└── 维护说明
```

### 6.4 复杂度评级

| 星级 | 描述 | 示例 |
|-----|------|------|
| ⭐ | 单一效果，无需依赖 | CSS 渐变、Flex 居中 |
| ⭐⭐ | 简单组件，1-2 个依赖 | Hover 效果、基础动画 |
| ⭐⭐⭐ | 中等复杂度，组合使用 | 骨架屏、表单验证 |
| ⭐⭐⭐⭐ | 复杂系统，多技术栈 | 虚拟列表、拖拽排序 |
| ⭐⭐⭐⭐⭐ | 完整解决方案 | 主题系统、权限管理 |

---

## 7. 知识文档模板

### 7.1 模板列表

| 模板 | 用途 | 文件位置 |
|-----|------|---------|
| distill-guide.md | 通用蒸馏指南（按 type 选对应章节） | templates/distill-guide.md |
| analyze-output.md | `/frontend <页面路径>` 输出模板 | templates/analyze-output.md |
| page-layout-template.md | 页面布局模式蒸馏专用模板 | templates/page-layout-template.md |

### 7.2 模板选择指南

```
知识类型 → 推荐模板
━━━━━━━━━━━━━━━━━━━━━━━━

技术实现模式 → distill-guide.md（pattern 章节）
经验总结 → distill-guide.md（best 章节）
问题记录 → distill-guide.md（pitfall 章节）
页面布局模式 → page-layout-template.md（独立流程）
页面分析方案 → analyze-output.md
```

---

## 8. 验证机制

### 8.1 验证检查清单

#### 代码完整性

```
□ 所有导入语句完整
□ 类型定义完整
□ 错误处理到位
□ 边界情况处理
```

#### 步骤清晰度

```
□ 每个步骤目的明确
□ 步骤顺序合理
□ 没有遗漏步骤
□ 没有模糊步骤
```

#### 依赖明确性

```
□ 依赖名称明确
□ 版本要求说明
□ 安装命令提供
□ 兼容性说明
```

#### 示例可运行

```
□ 代码无语法错误
□ 可直接复制使用
□ 包含完整上下文
□ 适当注释说明
```

### 8.2 验证报告格式

```markdown
## 验证报告

**文件**: [文件名]
**日期**: [验证日期]
**级别**: [验证级别]

### 验证结果

| 检查项 | 状态 | 说明 |
|-------|------|------|
| 结构完整性 | ✅ | - |
| 代码准确性 | ⚠️ | 缺少依赖版本 |
| 注意事项 | ✅ | - |

### 问题列表

1. **[中等]**: 缺少依赖版本说明
   - 位置: 快速使用部分
   - 建议: 添加 gsap 版本要求

### 验证通过

✅ / ❌
```

---

## 9. 贡献指南

### 9.1 如何贡献知识

```
贡献步骤:
1. 发现知识
   └── 在项目中遇到好的实现、踩坑或最佳实践

2. 触发蒸馏
   └── 使用 /frontend:distill 命令提取知识

3. 填写模板
   └── 按照对应模板填写内容

4. 提交验证
   └── 检查完整性和准确性

5. 保存同步
   └── 自动保存到知识库并同步
```

### 9.2 贡献类型

| 类型 | 描述 | 奖励 |
|-----|------|------|
| 新增模式 | 添加新的技术模式 | ⭐⭐⭐ |
| 完善现有 | 补充现有模式的示例或说明 | ⭐⭐ |
| 踩坑记录 | 记录有价值的问题和解决方案 | ⭐⭐ |
| 修正错误 | 修正过时或错误的内容 | ⭐ |
| 优化规范 | 改进知识规范或模板 | ⭐⭐ |

### 9.3 质量要求

```
好的贡献应该:
├── ✅ 有明确的价值（解决实际问题）
├── ✅ 内容完整（包含必要部分）
├── ✅ 代码可运行（无语法错误）
├── ✅ 说明清晰（易于理解）
├── ✅ 格式规范（符合模板）
└── ✅ 关联完整（链接相关知识）
```

---

## 📞 联系方式

如有问题或建议，请在项目中提交 Issue 或联系维护者。

---

## 📋 更新日志

| 版本 | 日期 | 更新内容 |
|-----|------|---------|
| 1.0 | 2024-01 | 初始版本，统一 Skill 命令系统 |
| 2.0 | 2026-09-17 | Skill 重命名 `frontend-knowledge` → `frontend`；合并 `/frontend:knowledge` + `/frontend:analyze` → `/frontend`（双分支）；新增 `/frontend:list`；新增自动触发模式；蒸馏类型新增 `page-layout`；五层召回架构（L1-L5） |

---

*最后更新: 2026-09-17*
