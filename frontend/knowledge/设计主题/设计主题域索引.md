***

title: "设计主题域索引"
description: "前端设计系统与主题配置经验索引页"
-------------------------------

# 设计主题域 - 模式索引

## 📂 目录结构

```
设计主题/
├── 设计原则-色彩与视觉.md             # 色彩基础与视觉感知原则
├── 设计令牌色彩系统.md                # CSS 变量色彩系统（base/surface/text/border/shadow）
├── 设计原则-排版规范.md               # 排版基础规范
├── 页面排版规范.md                    # Hero/Section/Panel/Form 组件系统
├── 圆角与形状规范.md                  # 4 级圆角系统（XL/L/M/S/Pill）
├── 间距比例系统.md                    # 8 级间距体系（8/12/14/16/18/20/24/32px）
├── 设计原则-移动端60-30-10配色.md     # 移动端 60/30/10 配色法则
├── 设计原则-移动端排版规范.md         # 移动端 1字体+4字号+2字重
├── 移动端字体规范.md                  # 14 级字号体系（18-88px）
├── 设计原则-移动端阴影与质感.md       # 柔阴影 + 染色阴影
├── 移动端峰值-终值情感设计.md         # Peak-End Rule 与情感设计
├── 移动端行业设计语言.md               # AI/金融/教育等行业惯例
├── 移动端卡片4层信息层级.md           # 主/次/辅助/元信息层级
├── UniApp移动端Token系统.md           # 颜色/间距/圆角/阴影/z-index/字体 token
├── CSS排版速查表.md                   # Tailwind 对照与速查
├── 可变字体与OpenType特性.md          # VF/OT 特性、等宽数字
├── 文本换行与截断.md                   # text-wrap、truncate
├── 拟物化UI设计.md                     # 表面配方、浮雕效果、质感
└── AI设计反模式.md                     # AI 生成式 UI 的常见反模式
```

> 说明：暗色模式/主题切换/阴影规范 等条目仍按需通过 `/frontend:distill` 生成后回填本表。

## 🎯 常见场景速查

| 需求场景   | 推荐模式   | 复杂度 |
| ------ | ------ | --- |
| 定义颜色变量 | 色彩变量   | ⭐   |
| 实现暗色模式 | 暗色模式实现 | ⭐⭐  |
| 主题切换   | 主题切换   | ⭐⭐  |
| 间距规范   | 间距比例   | ⭐   |
| 圆角规范   | 圆角规范   | ⭐   |
| 阴影效果   | 阴影规范   | ⭐   |
| 字体规范   | 字体规范   | ⭐   |
| 移动端配色 | 设计原则-移动端60-30-10配色 | ⭐ |
| 移动端排版 | 设计原则-移动端排版规范 | ⭐ |
| 移动端阴影 | 设计原则-移动端阴影与质感 | ⭐⭐ |
| 情感设计 | 移动端峰值-终值情感设计 | ⭐⭐ |
| 行业设计惯例 | 移动端行业设计语言 | ⭐ |
| 可变字体 | 可变字体与OpenType特性 | ⭐ |
| CSS排版 | CSS排版速查表 | ⭐ |
| 文本换行 | 文本换行与截断 | ⭐ |
| 拟物化 | 拟物化UI设计 | ⭐⭐ |
| UniApp Token | UniApp移动端Token系统 | ⭐⭐ |
| 卡片信息层级 | 移动端卡片4层信息层级 | ⭐ |
| 设计令牌色彩 | 设计令牌色彩系统 | ⭐⭐ |
| 圆角与形状 | 圆角与形状规范 | ⭐ |
| 移动端字体 | 移动端字体规范 | ⭐ |
| 间距比例系统 | 间距比例系统 | ⭐ |
| 页面组件排版 | 页面排版规范 | ⭐⭐ |

## 🔥 核心模式

### ⭐ 色彩变量定义

**CSS变量方案**

```css
:root {
  /* 主色 */
  --color-primary: #1890ff;
  --color-primary-light: #40a9ff;
  --color-primary-dark: #096dd9;

  /* 辅助色 */
  --color-success: #52c41a;
  --color-warning: #faad14;
  --color-error: #ff4d4f;
  --color-info: #1890ff;

  /* 中性色 */
  --color-text-primary: #262626;
  --color-text-secondary: #595959;
  --color-text-tertiary: #8c8c8c;
  --color-text-quaternary: #bfbfbf;

  /* 背景色 */
  --color-bg-base: #ffffff;
  --color-bg-container: #ffffff;
  --color-bg-elevated: #ffffff;
  --color-bg-layout: #f5f5f5;

  /* 边框 */
  --color-border: #d9d9d9;
  --color-border-secondary: #f0f0f0;
}
```

### ⭐ 暗色模式实现

```css
/* 基础暗色变量 */
[data-theme='dark'] {
  --color-bg-base: #141414;
  --color-bg-container: #1f1f1f;
  --color-text-primary: #ffffff;
  --color-text-secondary: rgba(255, 255, 255, 0.85);
  --color-border: #303030;
}
```

```tsx
// React 主题切换
import { useState, createContext, useContext } from 'react';

type Theme = 'light' | 'dark';

const ThemeContext = createContext<{
  theme: Theme;
  toggleTheme: () => void;
}>({ theme: 'light', toggleTheme: () => {} });

export const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState<Theme>('light');

  const toggleTheme = () => {
    const newTheme = theme === 'light' ? 'dark' : 'light';
    setTheme(newTheme);
    document.documentElement.setAttribute('data-theme', newTheme);
  };

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => useContext(ThemeContext);
```

### ⭐ 间距比例

```css
:root {
  /* 基于8px的比例系统 */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
}
```

### ⭐ 圆角规范

```css
:root {
  /* 统一圆角规范 */
  --radius-sm: 2px;      /* 小标签、小按钮 */
  --radius-md: 4px;      /* 按钮、输入框 */
  --radius-lg: 8px;      /* 卡片、面板 */
  --radius-xl: 12px;     /* 大卡片 */
  --radius-full: 9999px;  /* 圆形按钮、头像 */
}
```

### ⭐ 阴影规范

```css
:root {
  /* 阴影层级 */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
}
```

### ⭐ 字体规范

```css
:root {
  /* 字体族 */
  --font-sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'SF Mono', Monaco, 'Inconsolata', 'Roboto Mono', monospace;

  /* 字号 */
  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-lg: 18px;
  --text-xl: 20px;
  --text-2xl: 24px;
  --text-3xl: 30px;
  --text-4xl: 36px;

  /* 行高 */
  --leading-tight: 1.25;
  --leading-normal: 1.5;
  --leading-relaxed: 1.75;

  /* 字重 */
  --font-normal: 400;
  --font-medium: 500;
  --font-semibold: 600;
  --font-bold: 700;
}
```

### ⭐ 主题切换完整实现

```tsx
// 1. 定义主题类型
type Theme = 'light' | 'dark' | 'system';

// 2. 创建ThemeProvider
const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState<Theme>('system');

  useEffect(() => {
    const root = window.document.documentElement;

    const applyTheme = (newTheme: Theme) => {
      if (newTheme === 'system') {
        const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
          ? 'dark' : 'light';
        root.setAttribute('data-theme', systemTheme);
      } else {
        root.setAttribute('data-theme', newTheme);
      }
    };

    applyTheme(theme);

    // 监听系统主题变化
    const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
    const handleChange = () => applyTheme(theme);
    mediaQuery.addEventListener('change', handleChange);

    return () => mediaQuery.removeEventListener('change', handleChange);
  }, [theme]);

  return (
    <ThemeContext.Provider value={{ theme, setTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};
```

## 🏷️ 标签分类

| 标签         | 说明   | 相关模式      |
| ---------- | ---- | --------- |
| color      | 颜色   | 色彩变量、语义色  |
| design-token | 设计令牌 | 设计令牌色彩系统 |
| dark-mode  | 暗色模式 | 暗色模式实现    |
| theme      | 主题   | 主题切换      |
| spacing    | 间距   | 间距比例、间距比例系统 |
| typography | 字体   | 字体规范、字号系统、移动端字体规范 |
| radius     | 圆角与形状 | 圆角与形状规范 |
| border     | 边框   | 圆角规范、边框规范 |
| shadow     | 阴影   | 阴影规范      |
| mobile     | 移动端 | 移动端字体规范  |
| page-layout | 页面排版 | 页面排版规范    |

## 📝 待补充模式

- [ ] 设计令牌系统

- [ ] 组件变体系统

- [ ] 动画主题配置

- [ ] 多品牌主题

- [ ] 设计系统文档化

---

**最后更新**：2026-09-17

<br />
